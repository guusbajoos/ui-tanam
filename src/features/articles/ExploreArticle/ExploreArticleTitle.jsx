const ExploreArticleTitle = ({ title }) => {
	return (
		<div className=' flex items-center justify-center text-primary '>
			<h2 className='text-electric-blue text-4xl font-bold'>{title}</h2>
		</div>
	);
};

export default ExploreArticleTitle;
